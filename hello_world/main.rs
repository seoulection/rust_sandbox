// the "main" function is the first code that runs in every executable Rust program
fn main() {
    // println!(...) calls a macro
    // println(...) without the ! calls a function
    println!("Hello, world!");
}
